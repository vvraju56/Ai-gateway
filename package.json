{
  "name": "api-key-aggregator",
  "version": "1.0.0",
  "description": "Vercel-ready API key aggregator with serverless functions",
  "main": "api/proxy.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "start": "vercel dev",
    "deploy": "vercel --prod"
  },
  "keywords": ["api", "proxy", "key-rotation", "vercel", "serverless"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "vercel": "^32.4.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}